[package]
name = "beat_analyzer"
version = "1.2.0"
edition = "2021"

[dependencies]
rustfft = "6.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
symphonia = { version = "0.5", features = [
  "all-codecs",
  "all-formats"
] }
rayon = "1.10"

# ── Release: maximum throughput ───────────────────────────────
[lints]
workspace = true
