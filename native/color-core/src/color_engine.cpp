// Source-layout compatibility wrapper. CMake compiles the implementation at
// ../color_engine.cpp so older Premiere project files and the new library share
// exactly one production implementation.
#include "../color_engine.cpp"
